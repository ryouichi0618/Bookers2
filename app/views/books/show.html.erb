
<%= render 'layouts/new_book', book: @book, btn: 'Creat Book' %>

<div>
  <h1>Book detail</h1>
  
    <p>ユーザー画像</p>
    <p>ユーザーネーム</p>
    <p><%= link_to @book.title, book_path(@book.id) %></p>
    <p><%= @book.body %></p>
    
    <p><%= link_to "edit", edit_book_path(@book.id) %></p>  
    
    <p><%= link_to "destroy", book_path(@book.id), method: :delete %></p>
  
</div>